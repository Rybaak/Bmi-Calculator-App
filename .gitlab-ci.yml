image:
  name: buildpacksio/pack
  entrypoint: [""]
  
pack-job:
  script:
    - pack build registry.gitlab.com/maaciekk/bmi-calculator-app --builder gcr.io/buildpacks/builder:v3 -e "GOOGLE_ENTRYPOINT=python app.py" --publish
